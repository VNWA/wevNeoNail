/** @type {import('tailwindcss').Config} */
export default {
  content: [],
  theme: {
    extend: {
      colors: {
        primary: '#feb2be',
      },
      fontSize: {
        title: '2.5rem',
      },
      boxShadow: {
        bottom: '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)', // Bóng đổ chỉ ở phía dưới
      },
      padding: {
        padding: 'padding: 3px 0 3px 10px'
      }


    },
  },
  plugins: [],
}

